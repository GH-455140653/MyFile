// struction.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"
/*
struct stud
{
	char num[5],name[10];//学号，姓名
	int s[4];//四项成绩
	double ave;//平均分
};

void readrec(stud *p)
{
	int i,j,k=0;
	for(i=0;i<30;i++)
	{
		printf("请输入第%d名学生的姓名：",i+1);
		scanf("%s",p->name);
		printf("请输入第%d名学生的学号：",i+1);
		scanf("%s",p->num);
		int t=0;
		for(j=0;j<4;j++)
		{
			printf("请输入%s同学的第%d门成绩：",p->name ,++t);
			scanf("%d",&(p->s[j]));
			k=k+p->s[j];
		}
		p->ave=1.0*k/4;
		p++;
	}
}

void writerec(stud *p)
{
	int i,j,k;
	for(i=0;i<30;i++)
	{
		j=0;
		printf("\n第%d位同学的记录：\n",i+1);
		printf("姓名：%s\n学号：%s\n第%d项成绩：%d\n第%d项成绩：%d\n第%d项成绩：%d\n第%d项成绩：%d\n四科成绩的平均分：%lf\n",p->name,p->num,j+1,p->s[j],j+2,p->s[j+1],j+3,p->s[j+2],j+4,p->s[j+3],p->ave);
		p++;
	}
}
*/


int _tmain(int argc, _TCHAR* argv[])
{
	/*
	stud s[30];
	readrec(s);
	writerec(s);
	*/
	return 0;
}

